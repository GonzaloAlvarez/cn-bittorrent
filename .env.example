# Headscale + tailnet
HEADSCALE_DOMAIN=hs.gn.al
LAB_DOMAIN=lab.gn.al
TAILNET_DOMAIN=ts.gn.al
INFRA_VPS_TAILNET_IP=100.64.0.1
TORRENT_AUTHKEY=               # mint via: ssh hs.gn.al 'docker exec cloudnet-headscale-1 headscale preauthkeys create -u 2 --reusable --expiration 24h --tags tag:vpn-client'

# *arr / qbittorrent runtime
USER_ID=1000
GROUP_ID=1000
TIMEZONE=America/Los_Angeles

# Watchtower notifications (optional but recommended)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
ALERT_EMAIL=
